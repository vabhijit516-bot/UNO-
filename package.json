{
    "name": "uno-monorepo",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "test": "npm run test --workspace @uno/shared",
        "dev": "npm run dev --workspace @uno/client"
    }
}