{
    "name": "uno-monorepo",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build --workspace @uno/client",
        "test": "npm run test --workspace @uno/shared",
        "dev": "npm run dev --workspace @uno/client"
    }
}