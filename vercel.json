{
  "buildCommand": "npm run build --workspace @uno/client",
  "outputDirectory": "packages/client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
